# R session / user artefacts
.Rproj.user/
.Rhistory
.RData
.Ruserdata
*.Rproj

# Compiled code
*.o
*.so
*.dll
src/*.o
src/*.so

# Build / check artefacts
*.tar.gz
*.zip
*.Rcheck/
..Rcheck/
/doc/
/Meta/

# man/ and NAMESPACE are generated by roxygen2 but tracked in git
# so that remotes::install_github() works without needing devtools
